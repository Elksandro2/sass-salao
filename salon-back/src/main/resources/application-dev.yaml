spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5433/salon_dev}
    username: ${DB_USER:salon}
    password: ${DB_PASS:salon}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: ${DB_POOL_SIZE:5}
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  # spring.mail herdado do application.yaml via ${MAIL_*} — não repetir aqui.
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    encoding: UTF-8
    cache: false

security:
  jwt:
    secret: dev-secret-key-change-in-prod-must-be-at-least-256-bits-long!!
    access-token-expiration-ms: 900000 # 15 minutos
    # 180 dias — mesmo valor de prod, pra dev não expirar sessão o tempo todo durante testes.
    refresh-token-expiration-ms: ${JWT_REFRESH_EXPIRATION_MS:15552000000}

ai:
  # Chave AES-256 (base64) que cifra a API key de IA armazenada em tb_ai_config — nunca em texto puro no banco.
  config-encryption-key: ZGV2LWFpLWVuY3J5cHRpb24ta2V5LTMyLWJ5dGVzISE=

app:
  # Chave AES-256 (base64) que cifra dados pessoais sensíveis (CPF, chave PIX) em tb_staff_profile.
  # Separada da chave de IA de propósito: comprometer uma não compromete a outra.
  pii-encryption-key: I75V7+Y3dKf5oKexVJKiyDvrAFgWldgN+wc8ARi/NfI=
  # Pepper do HMAC-SHA256 do hash determinístico do CPF (checagem de duplicidade sem decifrar).
  pii-hash-pepper: sezDL9n7czVKGBhKfv2cSfX8H/QIcS78rExJfv0FN1s=

vapid:
  # Par de chaves VAPID de DEV — gerado uma única vez com `npx web-push generate-vapid-keys`.
  # ATENÇÃO: nunca regenere a chave privada de produção — isso invalida TODAS as subscriptions
  # de push já salvas no banco, forçando todo usuário a autorizar notificações de novo. Em
  # dev/test não tem problema recriar à vontade.
  public-key: BB0lhFj8jFWS9XnPtWGBBsp0dZ7-F_HGT_YVPYSjBoMkP6wPsqUr7m1y0gZQH1PaD2T8vT53DSOZfejjiQsj0qY
  private-key: zi8FsNAAo0HlDFm53KtOPhJBRSu2WM12UMyfdeOTIfk

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

logging:
  level:
    com.cristiane.salon: DEBUG
    org.springframework.security: DEBUG
